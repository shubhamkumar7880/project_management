$kind: http-request
name: logout
url: '{{server}}/api/v1/auth/logout'
headers:
  Authorization: Bearer {{accessToken}}
method: POST
headers:
  - key: Authorization
    value: 'Bearer {{accessToken}}'
order: 7000
